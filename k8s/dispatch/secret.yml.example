# Шаблон. Скопіюйте у secret.yml (він у .gitignore) і задайте власні значення:
#   cp k8s/dispatch/secret.yml.example k8s/dispatch/secret.yml
apiVersion: v1
kind: Secret
metadata:
  name: dispatch-db-secret
  namespace: delivery
  labels:
    app: dispatch-db
type: Opaque
stringData:
  POSTGRES_USER: CHANGE_ME
  POSTGRES_PASSWORD: CHANGE_ME
